#ifndef ASCII 	//如果其他文件已经做了这个记号，那么不执行#define.
#define ASCII 	//如果其他文件没有这个标记，那么就需要新做一个记号（唯一的）。
#include "msp.h"		
extern uint8_t  F8X6[92][6];//6*8字符
extern uint8_t  F8X16[];    //8*16大字符	
extern uint8_t  Hzk[][16];	//汉字字库
extern unsigned char nAsciiDot[];
#endif









